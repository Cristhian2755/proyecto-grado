{
  "name": "proyecto-grado",
  "private": true,
  "dependencies": {
    "dotenv": "^17.3.1"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "scripts": {
    "predev": "node scripts/prepare-dev.js",
    "dev:status": "node scripts/dev-status.js",
    "init-db": "node database/database.js",
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix database",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend start",
    "dev": "node ./node_modules/concurrently/dist/bin/concurrently.js -n BACKEND,FRONTEND,STATUS -c green,blue,magenta \"npm --prefix backend run dev\" \"npm --prefix frontend start\" \"npm run dev:status\""
  }
}
